// src/types/device.ts
export interface Device {
  id: string;
  name: string;
  modelId: string;
  supplierId: string;
  departmentId: string;
  purchaseDate: string;
  // ... các trường khác bạn có trong database
}
